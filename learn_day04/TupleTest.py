# Tuples are written with round brackets.
sampleTuple = ("apples", "oranges", "bananas")
# Access Element: sampleTuple[1]
print(sampleTuple[1])
# Iterate: for eachItem in sampleTuple:

# Element Exists? if "apple" in sampleTuple:

# Check Length: len(sampleTuple)

# You cannot add, remove, delete element from Tuple

# Delete Tuple: del sampleTuple